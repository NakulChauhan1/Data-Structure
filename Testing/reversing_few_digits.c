#include<stdio.h>
#include<math.h>
int main()
{
    int x, n, temp, r;
    printf("Enter number of digits\n");
    scanf("%d", &n);
   /* printf("Enter number of digits to be reversed\n");
    scanf("%d", &r);*/
    /*printf("Enter Number\n");
    scanf("%d", &x);

    temp = x % ( (int)pow( (double)10, n-2) );
    x = ( x/( (int)pow( (double)10, n-2) ) )%10 * ( (int)pow( (double)10, n-1) ) + ( x/( (int)pow( (double)10, n-1) ) ) * ( (int)pow( (double)10, n-2) )  + temp;
    printf("%d", x);*/






























    /*
    scanf("%d", &x);
    y = x;
    x = ( x / 100 )*100;
    x = x + ( y%10 *10 ) + (y/10)%10;
    printf("%d", x);                // reversing last two digits of a number
    */


    /*
    scanf("%d", &x);
    y = x;
    x = x%100;
    x = ( y/100 )%10 * 1000 + ( y/1000 )*100 + x;
    printf("%d", x);                // reversing first two digits of a number
    */


   /*
    printf("\n");
    scanf("%d", &x);
    for ( int i = 0; i < 4; i++ )
    {
        if ( i > 1 )
        {
            printf("%d", x%10);
            x/=10;
        }
        else
            x/=10;
    }*/
}
